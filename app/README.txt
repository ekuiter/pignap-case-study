Pignap
======

Developed using Apache Cordova. Requires Cordova CLI and Android SDK.

- `cordova run android`
- `cordova build android --release -- --keystore="android.keystore" --storePassword=android --alias=mykey`